import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
      img: {
        width: 300,
        height: 200
      },
      box: {
        alignItems: 'center',
      },
      text: {
        fontSize: 22,
        color: 'Black'
      }

});

export default styles;